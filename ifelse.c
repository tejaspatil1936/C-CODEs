#include <stdio.h>
int main() {
    int m;
        printf("enter marks");
        scanf("%d",&m);
            if(m > 35)
             {
                printf("pass");
             }
             else
             {
                printf("fail");
             }
             return 0;
}